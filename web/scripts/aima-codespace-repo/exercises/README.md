# Exercises

Small Python tasks aligned with AIMA-style **search** topics. Implement code in each `student.py` and run tests from the repo root:

```bash
pytest exercises/01_search_basics -q
```

Or run everything:

```bash
pytest exercises -q
```
