# 02 — Reachable node count

Given a directed graph as an adjacency list, implement `reachable_count(graph, start)` returning the number of distinct nodes reachable from `start`, **including** `start`.

Edit **`student.py`**, then run:

```bash
pytest exercises/02_reachable_count -q
```
